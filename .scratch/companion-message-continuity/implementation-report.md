# Companion correlated-message continuity implementation report

## Outcome

Implemented ticket `01 — Preserve correlated outgoing message continuity` for the complete spec.

- Correlated optimistic echoes, authoritative queue occurrences, and durable user/steering Chat nodes now share `submission:<rpcId>` as their Companion message-unit identity.
- Uncorrelated history keeps its authoritative Chat identity, and authoritative item IDs/projection keys remain unchanged for business data and media loading.
- Durable correlation suppresses stale queue material, while existing request-ID echo retirement and de-duplication remain owned by the Session controller.
- The in-memory fixture now performs an atomic correlated durable takeover. Playwright coverage captures the optimistic pure-text row and bubble, verifies both original DOM elements remain connected, checks text remains exactly once, and keeps the text-plus-two-image grouping/attachment replacement coverage.
- Projection tests cover idle durable takeover, queued/steering admission, stale queue overlap, image ownership, and uncorrelated identity behavior through test-owned snapshots.

## Acceptance coverage

Ticket `01` is complete: correlated request/RPC identities remain stable across transcript, queued, steering, and durable projections; no duplicate outgoing contribution is rendered; pure-text DOM identity is preserved; text-and-image sends stay one message unit while durable attachments replace local previews under existing cleanup; and all checks use fixture-owned/in-memory state.

## Changed paths and commit

Implementation commit: `e32097f` (`fix(companion): preserve correlated message continuity`). Changed paths are `src/projection.ts`, `fixture/main.ts`, `tests/projection.test.ts`, and `tests/e2e/fixture.spec.ts`.

## Verification

- `bun run typecheck` — passed.
- `bunx vitest run` — passed: 10 files, 83 tests.
- `bun run build` — passed: Host/client bundles and declarations built successfully.
- `bunx playwright test tests/e2e/fixture.spec.ts --workers=1 --reporter=dot` — passed: 60 tests, 2 intentional skips across desktop and Pixel 7a fixtures.
- `git diff --check` — passed.

The browser suite used only the local Vite fixture and in-memory test state. Tracked screenshot artifacts regenerated by Playwright were restored after each verification run; no live Session, profile, credential, or production state was touched. Code review and deployment remain outside this implementation run.

## Changed-LOC variance

Against fixed point `4cef10f`, excluding this report and generated artifacts, the implementation has 94 additions and 23 deletions: product code 21/7, fixture support 12/10, and tests 61/6. The product net change is within the 8–20-line estimate; the test/fixture additions are slightly above the 25–55-line estimate because the implementation verifies both queue placements, stale authoritative overlap, same-node DOM handles, and durable image replacement explicitly. The total remains a focused single-slice change with no transport, persistence, compatibility, or upstream-package work.

## Remaining concerns

None within the stated delivery boundary. Session admission/retirement scheduling, transport behavior, persistence, image-element identity, deployment, and code review remain intentionally unchanged or out of scope.
